package intermediate

import (
	"fmt"
	"math"
)

func maths() {
	pi := math.Pi
	e := math.E
	phi := math.Phi // the golden ratio
	// also has log fns, exponentials, sine, cosine trignometric functions
	// works with float64 values
	fmt.Println(phi, pi, e)
}
